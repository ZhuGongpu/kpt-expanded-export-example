version: 2.1
orbs:
  kpt:
    executors:
      kpt-container:
        docker:
          - image: gongpu/kpt:docker
    commands:
      run-kpt-functions:
        steps:
          - run: kpt fn run resources --fn-path functions.yaml
    jobs:
      run-functions:
        executor: kpt-container
        steps:
          - checkout
          - setup_remote_docker
          - run-kpt-functions
workflows:
  main:
    jobs:
      - kpt/run-functions
