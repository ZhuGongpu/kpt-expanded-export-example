version: 2.1
orbs:
  kpt:
    executors:
      kpt-container:
        docker:
          - image: gongpu/kpt
    commands:
      run-kpt-functions:
        steps:
          - run: kpt fn run .
    jobs:
      run-kpt-functions:
        executor: kpt-container
        steps:
          - run-kpt-functions
workflows:
  main:
    jobs:
      - kpt/run-kpt-functions
